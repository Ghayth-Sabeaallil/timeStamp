export function switchScene(currentView: HTMLElement, nextView: HTMLElement){
   currentView!.classList.add("hide");
   nextView!.classList.remove("hide");

    if(nextView.classList.contains("break-view")){
        
    }


} 

